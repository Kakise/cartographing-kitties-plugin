# Plan Tools

Generated from the FastMCP tool registry. Do not edit this file by hand; update
tool signatures/docstrings, then run `uv run python scripts/generate_tool_reference.py`.

## `plan_audit`

Validate one plan (when *path* is given) or every plan under ``docs/plans/``.

Returns ``{"ok": bool, "errors": [...]}`` where each error is a rendered
``"<path>: <message>"`` string.

### Parameters

| Name | Required | Type | Default |
|---|---:|---|---|
| `path` | no | `string | null` | `None` |

### Input Schema

```json
{
  "properties": {
    "path": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Path"
    }
  },
  "title": "plan_auditArguments",
  "type": "object"
}
```

### Output Schema

```json
null
```

## `plan_create`

Create a new plan document at *path* with *title*, *type*, and *units*.

``units`` is a list of dicts; each needs at least an ``id`` and ``title``
and may set ``state`` (defaults to ``pending``). Refuses to overwrite an
existing file. Returns ``{"ok": True, "path": ..., "units": [...]}``.

### Parameters

| Name | Required | Type | Default |
|---|---:|---|---|
| `path` | yes | `string` | `None` |
| `title` | yes | `string` | `None` |
| `type` | yes | `string` | `None` |
| `units` | yes | `array` | `None` |

### Input Schema

```json
{
  "properties": {
    "path": {
      "title": "Path",
      "type": "string"
    },
    "title": {
      "title": "Title",
      "type": "string"
    },
    "type": {
      "title": "Type",
      "type": "string"
    },
    "units": {
      "items": {
        "additionalProperties": true,
        "type": "object"
      },
      "title": "Units",
      "type": "array"
    }
  },
  "required": [
    "path",
    "title",
    "type",
    "units"
  ],
  "title": "plan_createArguments",
  "type": "object"
}
```

### Output Schema

```json
null
```

## `plan_set_status`

Set the plan-level ``status`` for the plan at *path* (atomic, guarded).

Pass ``implemented_in`` for ``status=complete``, ``superseded_by`` for
``status=superseded``, or ``abandoned_reason`` for ``status=abandoned``.
Returns ``{"ok": True, "status": ..., "units": [...]}`` or an error dict.

### Parameters

| Name | Required | Type | Default |
|---|---:|---|---|
| `path` | yes | `string` | `None` |
| `status` | yes | `string` | `None` |
| `implemented_in` | no | `string | null` | `None` |
| `superseded_by` | no | `string | null` | `None` |
| `abandoned_reason` | no | `string | null` | `None` |

### Input Schema

```json
{
  "properties": {
    "path": {
      "title": "Path",
      "type": "string"
    },
    "status": {
      "title": "Status",
      "type": "string"
    },
    "implemented_in": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Implemented In"
    },
    "superseded_by": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Superseded By"
    },
    "abandoned_reason": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Abandoned Reason"
    }
  },
  "required": [
    "path",
    "status"
  ],
  "title": "plan_set_statusArguments",
  "type": "object"
}
```

### Output Schema

```json
null
```

## `plan_set_unit_state`

Set *unit_id*'s ``state`` in the plan at *path* (atomic, conflict-guarded).

``commit`` records ``implemented_in`` (for ``state=complete``); ``reason``
records the skipped reason (for ``state=skipped``). Returns ``{"ok": True,
"status": <rollup>, "units": [...]}`` or an error dict on validation
failure / concurrent change.

### Parameters

| Name | Required | Type | Default |
|---|---:|---|---|
| `path` | yes | `string` | `None` |
| `unit_id` | yes | `integer` | `None` |
| `state` | yes | `string` | `None` |
| `commit` | no | `string | null` | `None` |
| `reason` | no | `string | null` | `None` |

### Input Schema

```json
{
  "properties": {
    "path": {
      "title": "Path",
      "type": "string"
    },
    "unit_id": {
      "title": "Unit Id",
      "type": "integer"
    },
    "state": {
      "title": "State",
      "type": "string"
    },
    "commit": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Commit"
    },
    "reason": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Reason"
    }
  },
  "required": [
    "path",
    "unit_id",
    "state"
  ],
  "title": "plan_set_unit_stateArguments",
  "type": "object"
}
```

### Output Schema

```json
null
```

## `plan_status`

Return the rollup status and per-unit states for the plan at *path*.

Shape: ``{"status": <rollup>, "units": [{"id", "title", "state",
"implemented_in"}, ...]}``.

### Parameters

| Name | Required | Type | Default |
|---|---:|---|---|
| `path` | yes | `string` | `None` |

### Input Schema

```json
{
  "properties": {
    "path": {
      "title": "Path",
      "type": "string"
    }
  },
  "required": [
    "path"
  ],
  "title": "plan_statusArguments",
  "type": "object"
}
```

### Output Schema

```json
null
```

