---
description: Show graph, annotation, and memory status
---

Use the Cartographing Kittens MCP server to inspect current project status.

Call `annotation_status()`, `query_litter_box()` with no filters, and `query_treat_box()` with no filters.

Present:
- files indexed and node count
- annotation coverage
- litter-box entry counts
- treat-box entry counts
